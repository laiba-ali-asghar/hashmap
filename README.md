# hashmap
This project contains the implement of hashmap in cpp using string . It has functions like insert , remove , search etc. It counts the ASCII of the strings and place them in the array by passing them through the function "getHashValue".
